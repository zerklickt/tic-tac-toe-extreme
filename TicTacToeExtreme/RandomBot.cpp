#include "pch.h"
#include <random>

#include "RandomBot.hpp"

int RandomBot::m_botId = 1;

/*
// places a chip in a random column
int RandomBot::makeMove(const std::string message, Playground playground)
{
	srand(time(NULL));
	int number = 1 + rand() % 7;
	return number;
}
*/
