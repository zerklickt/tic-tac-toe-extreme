#include "pch.hpp"
#include <random>

#include "SmartBot.hpp"

int SmartBot::m_botId = 1;

/*
// places a chip in a random column
int SmartBot::makeMove(const std::string message, Playground playground)
{
	srand(time(NULL));
	int number = 1 + rand() % 7;
	return number;
}
*/

